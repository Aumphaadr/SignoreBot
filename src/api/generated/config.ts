// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

export type AccountInfo = { login: string, userId: string, displayName: string, };

/**
 * Несекретная информация об авторизованных аккаунтах.
 */
export type Accounts = { broadcaster: AccountInfo | null, bot: AccountInfo | null, 
/**
 * Бот — тот же аккаунт, что и стример: один токен с объединёнными правами.
 */
sameAccount: boolean, };

/**
 * Поведение самого приложения (окно, трей).
 */
export type AppSettings = { 
/**
 * Закрытие окна прячет его в трей (бот работает дальше); иначе — выход.
 */
closeToTray: boolean, 
/**
 * Сколько секунд висит уведомление в панели (нижняя планка).
 */
notificationSeconds: number, 
/**
 * Уведомления не исчезают сами — только по крестику.
 */
notificationsSticky: boolean, 
/**
 * Масштаб панели, % (100 — как есть). Крупнее текст и значки для слабого зрения.
 */
uiZoom: number, };

export type BanWord = { word: string, kind: BanWordKind, 
/**
 * Сгенерированные варианты написания (для показа в UI; матчинг пересчитывает сам).
 */
aliases: Array<string>, };

export type BanWordKind = "hard" | "soft";

export type BanwordSettings = { words: Array<BanWord>, 
/**
 * Не проверять сообщения модераторов и стримера (их всё равно нельзя удалить).
 */
skipPrivileged: boolean, };

export type ChatResponse = { enabled: boolean, components: Array<Component>, };

export type Command = { id: string, enabled: boolean, 
/**
 * Имя без `!`, в нижнем регистре.
 */
name: string, 
/**
 * Алиасы без `!`, в нижнем регистре.
 */
aliases: Array<string>, 
/**
 * `broadcaster` | `moderators` | `vips` | `subscribers` | `everyone` | `user:<login>`.
 * Пусто — доступно всем.
 */
permissions: Array<string>, 
/**
 * Кулдаун между срабатываниями, с (0 — нет).
 */
cooldownSec: number, 
/**
 * Кулдаун на одного зрителя, с (0 — нет).
 */
cooldownUserSec: number, 
/**
 * Отвечать реплаем на сообщение зрителя, а не обычным сообщением.
 */
reply: boolean, response: Response, };

/**
 * Элемент чат-сообщения. Сообщение = конкатенация без разделителей.
 */
export type Component = { "type": "static", value: string, } | { "type": "author" } | { "type": "target" } | { "type": "randomViewer" } | { "type": "random", min: number, max: number, } | { "type": "phrase", phrases: Array<string>, } | { "type": "space" } | { "type": "variable", name: string, };

export type Config = { version: number, twitch: TwitchSettings, network: NetworkSettings, accounts: Accounts, overlaySettings: OverlaySettings, commands: Array<Command>, rewards: Array<Reward>, events: { [key in string]?: EventReaction }, periodicEvents: Array<PeriodicEvent>, shoutout: ShoutoutSettings, banwords: BanwordSettings, overlays: Array<Overlay>, obs: ObsSettings, notes: Array<Note>, updates: UpdateSettings, app: AppSettings, };

export type EventReaction = { enabled: boolean, 
/**
 * Для `subscribe`: не реагировать на подарочные подписки (о них сообщает `giftSub`).
 */
skipGifted: boolean, response: Response, };

export type FontSettings = { fontFamily?: string, fontSize?: number, fontWeight?: string, fontStyle?: string, color?: string, };

export type MediaAnimation = { 
/**
 * none | fadeIn | fadeInLeft | fadeInRight | fadeInTop | fadeInBottom | scaleIn
 */
enter: string, 
/**
 * none | fadeOut | fadeOutLeft | fadeOutRight | fadeOutTop | fadeOutBottom | scaleOut
 */
exit: string, enterDuration: number, exitDuration: number, };

export type MediaResponse = { enabled: boolean, 
/**
 * Имя файла в каталоге медиа.
 */
file: string, 
/**
 * Вторичный файл: звук к картинке или картинка к звуку.
 */
secondaryFile: string, 
/**
 * 0..=100
 */
volume: number, 
/**
 * id оверлея; `None` — все оверлеи.
 */
overlay: string | null, queueMode: QueueMode, 
/**
 * CSS `mix-blend-mode` или "none".
 */
chromakey: string, 
/**
 * Длительность показа картинки, с; `None` — из общих настроек.
 */
imageDurationSec: number | null, animation: MediaAnimation, text: MediaText, };

export type MediaText = { enabled: boolean, content: string, 
/**
 * overlay | above | below | left | right
 */
position: string, 
/**
 * none | bounce | pulse | rubberBand | tada | wave | wiggle | wobble
 */
animation: string, animationAmplitude: number, font: FontSettings, };

export type MigrationReport = { fromVersion: number, notes: Array<string>, };

export type NetworkSettings = { 
/**
 * Порт HTTP-сервера оверлеев (страницы, медиа, WebSocket на том же порту).
 */
httpPort: number, 
/**
 * Слушать все интерфейсы (доступ из LAN для OBS на другой машине).
 */
allowLan: boolean, 
/**
 * Ключ доступа к оверлеям: `/overlay/<path>?key=…`. Генерируется при первом запуске.
 */
overlayKey: string, 
/**
 * Показывать/копировать URL оверлеев с `127.0.0.1` (OBS на этом же
 * компьютере): так работает Service Worker и порядок запуска OBS/бота не важен.
 */
preferLocalhostUrls: boolean, };

export type Note = { id: string, text: string, status: NoteStatus, createdAt: string, updatedAt: string, };

export type NoteStatus = "active" | "done" | "cancelled";

export type ObsBrowserSource = { overlayPath: string, inputName: string, };

export type ObsSettings = { enabled: boolean, url: string, password: string, autoRefresh: boolean, browserSources: Array<ObsBrowserSource>, };

export type Overlay = { id: string, name: string, 
/**
 * Сегмент URL: `[a-z0-9-_]+`, уникален.
 */
path: string, 
/**
 * Что сделать, если медиа пришло, а оверлей не подключён: текст в чат
 * и/или медиа на другой оверлей. Когда задано, медиа для этого оверлея
 * не ждёт в отложенной очереди — срабатывает эта реакция.
 */
fallback: Response | null, 
/**
 * Резерв включён (состав хранится и в выключенном состоянии).
 */
fallbackEnabled: boolean, };

export type OverlaySettings = { 
/**
 * Пауза между элементами очереди на оверлее, мс.
 */
pauseBetweenMs: number, 
/**
 * Длительность показа картинки по умолчанию, с.
 */
imageDurationSec: number, 
/**
 * Антиспам: тот же файл от того же пользователя в этом окне (мс) отбрасывается.
 * 0 — выключено.
 */
antispamWindowMs: number, };

export type PeriodicEvent = { id: string, name: string, enabled: boolean, 
/**
 * ≥ 10
 */
intervalSec: number, 
/**
 * < interval
 */
offsetSec: number, 
/**
 * Цвет на таймлайне (#hex) или пусто.
 */
color: string, 
/**
 * Сработать сразу при запуске бота (иначе первое срабатывание — через offset,
 * но не раньше чем через интервал с момента запуска при offset=0).
 */
fireOnStart: boolean, response: Response, };

export type QueueMode = "queue" | "immediate";

export type RaidShoutoutMode = "none" | "listed" | "unlisted" | "all";

export type Response = { chat: ChatResponse, media: MediaResponse, };

export type Reward = { id: string, enabled: boolean, 
/**
 * UUID награды в Twitch.
 */
rewardId: string, rewardTitle: string, response: Response, 
/**
 * Награда создана через бота (Twitch разрешает менять её и возвращать
 * баллы только приложению-создателю).
 */
managed: boolean, 
/**
 * Если медиа не доставлено (оверлей выключен) — вернуть баллы зрителю.
 * Работает только для `managed`; тогда бот сам закрывает и удачные
 * погашения (FULFILLED), чтобы они не копились в очереди запросов.
 */
refundIfUnavailable: boolean, 
/**
 * Id награды, с которой сделана управляемая копия (для подсказок).
 */
originalRewardId: string | null, };

export type ShoutoutSettings = { 
/**
 * Логины (нижний регистр) для авто-shoutout по первому сообщению за сессию.
 */
autoList: Array<string>, raidMode: RaidShoutoutMode, 
/**
 * Пауза между shoutout, с (Twitch: минимум 2 минуты).
 */
cooldownSec: number, };

export type TwitchSettings = { 
/**
 * Client ID публичного Twitch-приложения (Device Code Grant Flow).
 */
clientId: string, };

export type UpdateSettings = { 
/**
 * Репозиторий GitHub с релизами (для форков — свой).
 */
repoUrl: string, checkOnStart: boolean, };
