// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { MigrationReport } from "./config";

export type AccountKind = "broadcaster" | "bot";

export type AccountStatus = { 
/**
 * none | pending | authorized | invalid
 */
state: string, login: string | null, userId: string | null, scopes: Array<string>, missingScopes: Array<string>, 
/**
 * Unix-время истечения access-токена, с.
 */
expiresAt: number, 
/**
 * Ошибка последней операции (для UI).
 */
error: string | null, 
/**
 * Активная Device-Code-авторизация.
 */
device: DeviceCode | null, };

export type ChannelReward = { id: string, title: string, cost: number, isEnabled: boolean, isPaused: boolean, requiresInput: boolean, backgroundColor: string, image: string | null, prompt: string, 
/**
 * Погашения проходят мимо очереди запросов (сразу FULFILLED) — вернуть баллы нельзя.
 */
skipQueue: boolean, cooldownSeconds: number | null, maxPerStream: number | null, maxPerUserPerStream: number | null, 
/**
 * Создана нашим приложением — можно менять и возвращать баллы.
 */
isManaged: boolean, };

export type CoreStatus = { broadcaster: AccountStatus, bot: AccountStatus, 
/**
 * Рантайм (EventSub, таймеры) запущен.
 */
running: boolean, eventsub: EventSubStatus, server: ServerStatus, overlays: Array<OverlayStatusItem>, secretsBackend: string, dataDir: string, migration: MigrationReport | null, version: string, 
/**
 * Бот работает, а часть оверлеев не открыта ни одним Browser Source.
 */
overlayAlert: string | null, 
/**
 * Последняя проверка обновлений (при запуске, раз в 12 часов и по кнопке).
 */
update: UpdateInfo | null, };

export type DataDirInfo = { current: string, default: string, source: PathSource, };

export type DeviceCode = { deviceCode: string, userCode: string, verificationUri: string, expiresIn: number, interval: number, };

export type DoneItem = { username: string, sources: Array<ShoutoutSource>, };

export type EventSubStatus = { connected: boolean, sessionId: string | null, subscriptions: number, };

export type ImportResult = { report: MigrationReport, mediaImported: number, mediaErrors: Array<string>, };

export type LogEntry = { 
/**
 * Unix-время, мс.
 */
ts: number, 
/**
 * `info` | `warn` | `error` | `debug`
 */
level: string, 
/**
 * Категория (модуль).
 */
target: string, message: string, };

export type ManagedCopyResult = { newRewardId: string, newTitle: string, originalRewardId: string, originalTitle: string, rewardsUrl: string, };

export type MediaFile = { name: string, size: number, 
/**
 * Unix-время изменения, мс.
 */
modified: number, 
/**
 * image | video | audio | unknown
 */
kind: string, 
/**
 * Используется ли в конфиге.
 */
used: boolean, };

export type MediaImportResult = { files: Array<MediaFile>, errors: Array<string>, };

export type ObsSource = { inputName: string, inputKind: string, 
/**
 * Текущий URL (если это browser source).
 */
url: string | null, };

export type OverlayConnection = { path: string, remote: string, 
/**
 * Unix-время подключения, мс.
 */
since: number, };

export type OverlayStatusItem = { id: string, name: string, path: string, url: string, connected: boolean, connections: number, pending: number, };

export type PathSource = "default" | "pointer" | "env";

/**
 * Погашение награды, которое бот не смог выполнить (оверлей был выключен),
 * либо закрыл сам. Показывается на «Баллах канала».
 */
export type PendingRedemption = { redemptionId: string, rewardId: string, rewardTitle: string, user: string, at: number, 
/**
 * Почему не выполнено (какой оверлей был недоступен).
 */
reason: string, 
/**
 * pending | refunded (бот вернул) | canceled (вернули в Twitch) | fulfilled | dismissed
 */
status: string, };

export type ProbeResult = { file: string, size: number, kind: string, extension: string, codec: string | null, warnings: Array<string>, };

export type QueueItem = { id: number, username: string, source: ShoutoutSource, attempts: number, };

export type ServerStatus = { running: boolean, address: string | null, port: number, allowLan: boolean, lanIp: string, error: string | null, overlayKey: string, };

export type ShoutoutSource = "message" | "raid" | "manual";

export type ShoutoutStatus = { queue: Array<QueueItem>, done: Array<DoneItem>, 
/**
 * Осталось до конца кулдауна, мс.
 */
cooldownRemainingMs: number, processing: boolean, currentId: number, };

export type TimerStatus = { id: string, name: string, 
/**
 * Секунд до следующего срабатывания.
 */
nextInSec: number, };

export type UpdateAsset = { name: string, url: string, size: number, };

export type UpdateInfo = { current: string, latest: string | null, isNewer: boolean, url: string | null, publishedAt: string | null, notes: string | null, 
/**
 * Ссылки на файлы релиза.
 */
assets: Array<UpdateAsset>, 
/**
 * Unix-время проверки, мс.
 */
checkedAt: number, };

export type ViewersInfo = { cached: Array<string>, recent: Array<string>, };
