[env]
# ts-rs пишет TypeScript-типы прямо во фронтенд
TS_RS_EXPORT_DIR = { value = "../src/api/generated", relative = true }

# ts-rs пишет bindings файлом целиком: генерировать ТОЛЬКО полным `cargo test`, не отфильтрованным.
